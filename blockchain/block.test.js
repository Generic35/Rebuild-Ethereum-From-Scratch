const Block = require('./block');
const { keccakHash } = require('../util');

describe('Block', () => {
  describe('calculateBlockTargetHash()', () => {

  });

  describe('mineBlock()', () => {

  });
});
